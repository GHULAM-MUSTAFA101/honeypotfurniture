{% comment %}
  Pick Your Fabric & Colour Section
  Fully customizable and responsive
{% endcomment %}

<section class="fabric-section">
  <div class="fabric-container">
    <div class="fabric-header">
      {% if section.settings.heading_number != blank %}
        <h2 class="fabric-number">{{ section.settings.heading_number }}</h2>
      {% endif %}
      {% if section.settings.heading != blank %}
        <h3 class="fabric-title">{{ section.settings.heading }}</h3>
      {% endif %}
      <ul class="fabric-points">
        {% if section.settings.point_1 != blank %}
          <li>{{ section.settings.point_1 }}</li>
        {% endif %}
        {% if section.settings.point_2 != blank %}
          <li>{{ section.settings.point_2 }}</li>
        {% endif %}
      </ul>
    </div>

    <div class="fabric-grid">
      {% for block in section.blocks %}
        <div class="fabric-item">
          {% if block.settings.image != blank %}
            <div class="fabric-image">
              <img src="{{ block.settings.image | img_url: '400x400' }}" alt="{{ block.settings.title | escape }}">
            </div>
          {% endif %}
          {% if block.settings.title != blank %}
            <p class="fabric-name">{{ block.settings.title }}</p>
          {% endif %}
        </div>
      {% endfor %}
    </div>

    <div class="fabric-footer">
      {% if section.settings.footer_title != blank %}
        <p class="fabric-subtitle">{{ section.settings.footer_title }}</p>
      {% endif %}
      <ul class="fabric-list">
        {% if section.settings.list_item_1 != blank %}
          <li>{{ section.settings.list_item_1 }}</li>
        {% endif %}
        {% if section.settings.list_item_2 != blank %}
          <li>{{ section.settings.list_item_2 }}</li>
        {% endif %}
        {% if section.settings.list_item_3 != blank %}
          <li>{{ section.settings.list_item_3 }}</li>
        {% endif %}
      </ul>
    </div>
  </div>
</section>

<style>
.fabric-section {
  background: #fff;
  color: #000;
  padding: 60px 20px;
}

.fabric-container {
  max-width: 1200px;
  margin: 0 auto;
}

.fabric-header {
  text-align: center;
  margin-bottom: 40px;
}

.fabric-number {
  font-size: 22px;
  font-weight: 700;
  color: #000;
  margin-bottom: 8px;
}

.fabric-title {
  font-size: 32px;
  font-weight: 800;
  color: #000;
  margin-bottom: 20px;
}

.fabric-points {
  list-style: disc;
  padding-left: 20px;
  margin: 0 auto 30px;
  display: inline-block;
  text-align: left;
}

.fabric-points li {
  font-size: 16px;
  color: #000;
  margin-bottom: 8px;
}

.fabric-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 20px;
  margin-bottom: 50px;
}

.fabric-item {
  text-align: center;
  border: 1px solid #e5e5e5;
  background: #fff;
  transition: all 0.3s ease;
}

.fabric-item:hover {
  transform: translateY(-5px);
  box-shadow: 0 4px 10px rgba(0,0,0,0.1);
}

.fabric-image {
  width: 100%;
  overflow: hidden;
}

.fabric-image img {
  width: 100%;
  height: auto;
  display: block;
}

.fabric-name {
  font-size: 15px;
  font-weight: 600;
  color: #000;
  padding: 10px;
  border-top: 1px solid #e5e5e5;
  margin: 0;
}

.fabric-footer {
  text-align: left;
  margin-top: 40px;
}

.fabric-subtitle {
  font-weight: 700;
  font-size: 18px;
  color: #000;
  margin-bottom: 12px;
}

.fabric-list {
  list-style: disc;
  padding-left: 20px;
}

.fabric-list li {
  font-size: 15px;
  color: #000;
  margin-bottom: 6px;
}

/* Responsive */
@media (max-width: 992px) {
  .fabric-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

@media (max-width: 768px) {
  .fabric-title {
    font-size: 24px;
  }
  .fabric-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}
</style>

{% schema %}
{
  "name": "Fabric & Colour Picker",
  "settings": [
    {
      "type": "text",
      "id": "heading_number",
      "label": "Heading Number",
      "default": "05"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Main Heading",
      "default": "Pick Your Fabric & Colour"
    },
    {
      "type": "text",
      "id": "point_1",
      "label": "First Point",
      "default": "Over 30 fabric types, each with more than 30 colours."
    },
    {
      "type": "text",
      "id": "point_2",
      "label": "Second Point",
      "default": "Ten curated looks make it easy to find the texture and style that fit your home."
    },
    {
      "type": "text",
      "id": "footer_title",
      "label": "Footer Title",
      "default": "Our Price Ranges are clearly grouped into five categories:"
    },
    {
      "type": "text",
      "id": "list_item_1",
      "label": "List Item 1",
      "default": "Honeypot Classics"
    },
    {
      "type": "text",
      "id": "list_item_2",
      "label": "List Item 2",
      "default": "Honeypot Signatures"
    },
    {
      "type": "text",
      "id": "list_item_3",
      "label": "List Item 3",
      "default": "Performance Select / Luxe / Elite (all with FibreGuard for stain and pet protection)"
    }
  ],
  "blocks": [
    {
      "type": "fabric",
      "name": "Fabric Item",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Fabric Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Fabric Name"
        }
      ]
    }
  ],
  "max_blocks": 10,
  "presets": [
    {
      "name": "Fabric & Colour Picker",
      "category": "Custom Sections"
    }
  ]
}
{% endschema %}
